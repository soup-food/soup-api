events {}

http {
    include       mime.types;
    default_type  application/octet-stream;

    server {
        listen 80;


        location / {
            root /usr/share/nginx/html;
            try_files $uri =404;
        }

        location /api/ {
            proxy_pass http://sus_food_backend:8000/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}
